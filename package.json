{
  "name": "aidev-prompt",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "generate": "bun run generate.ts",
    "sync": "bun run generate.ts && bun run sync.ts"
  }
}
